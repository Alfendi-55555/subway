# 05-20 수정 내용 - 열차 업데이트 로직과 시뮬레이션 스코어 공식 조정
1. 배경
- 기존 "열차 추가" 시나리오는 사실상 패널티가 없었음(열차 지연 등 고려 안했기에)
- "무정차 통과" 시나리오는 사실 그 역에 아예 승객 순환이 없으므로 채택 점수가 매우 낮은 구조적 문제가 있었음
=> 시나리오 평가 기준을 전체 교통망 측면으로 확장 (즉 조작은 특정 역에 가하되, 전체 병목 해소 정도를 보는 것)

2. 주요 변경 사항
- stop_timer 추가: 역에 대기 중인 승객 수가 많을수록 열차의 정차시간이 증가합니다.
-- required_stop_time = 1500 + min(len(current_station) * 50, 5000): 지연 시간 공식. #꺠알같이 station 파일에서 정의된 매직 메서드 __len__을 활용했다!
- 관련한 시각화 지표 변경사항을 반영하였습니다.